package com.ameri.converter.user.manager;

import com.ameri.converter.Converter;
import com.ameri.objects.classes.user.manager.Tag;

public class TagConverter extends Converter<Tag> {

    public TagConverter(Class<Tag> typeConverter) {
        super(typeConverter);
    }
}
