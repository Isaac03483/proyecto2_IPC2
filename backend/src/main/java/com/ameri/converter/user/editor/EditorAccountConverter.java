package com.ameri.converter.user.editor;

import com.ameri.converter.Converter;
import com.ameri.objects.classes.user.editor.EditorAccount;

public class EditorAccountConverter extends Converter<EditorAccount> {

    public EditorAccountConverter(Class<EditorAccount> typeConverter) {
        super(typeConverter);
    }
}
