package com.ameri.converter.user.editor;

import com.ameri.converter.Converter;
import com.ameri.objects.classes.user.editor.EditorTag;

public class EditorTagConverter extends Converter<EditorTag> {

    public EditorTagConverter(Class<EditorTag> typeConverter) {
        super(typeConverter);
    }
}
